<configuration>

  <logger name="com.socrata" level="DEBUG" />

  <appender name="FILE" class="ch.qos.logback.core.FileAppender">
    <file>sbt-test.log</file>

    <!-- truncate the logfile every time we start the test -->
    <append>false</append>

    <encoder>
      <pattern>[%date{ISO8601}] %-5level %logger{26} [%thread{20}] - %msg%n</pattern>
    </encoder>

  </appender>

  <root level="INFO">
    <appender-ref ref="FILE" />
  </root>
</configuration>
